-#  This file is app/views/mass_params/choose.html.haml

%h1 Choose Existing Parameters File


%div{:id => 'center-parent'}
  = render "general/sidebar"

  %div{:class => 'card', :id => 'center'}
    %div{:id => 'tabs'}
      = button_to 'Upload Parameters', new_mass_param_path, :class => 'center-btn btn tab', :form => {:class => 'tab_buttons'}, :method => :get
      = button_to 'Choose Existing Files', choose_params_path, :class => 'center-btn btn tab', :form => {:class => 'tab_buttons'}, :method => :get, :disabled => @disabled_params

    %div{:id => 'content'}
      %div
        = form_tag update_params_path, :method => :post, :id => "data_form" do
          %p{:class => 'msg'} #{@message}
          %p <br />
          - @all_params.each do |data|
            %div{:id => 'tabs'}
              = radio_button_tag "data_id", data.id
              = data.get_title
          %p <br />
          = submit_tag 'Save and Next', :id => 'data_submit', :class => 'center-btn btn'
